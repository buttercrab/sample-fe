import { createContext } from '../../utils/create-context';
export const [EditableProvider, useEditableContext] = createContext({
    hookName: 'useEditableContext',
    providerName: '<EditableProvider />',
});
