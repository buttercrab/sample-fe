import { createContext } from '../../utils/create-context';
export const [PaginationProvider, usePaginationContext] = createContext({
    hookName: 'usePaginationContext',
    providerName: '<PaginationProvider />',
});
