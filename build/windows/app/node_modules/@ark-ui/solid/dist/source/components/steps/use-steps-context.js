import { createContext } from '../../utils/create-context';
export const [StepsProvider, useStepsContext] = createContext({
    hookName: 'useStepsContext',
    providerName: '<StepsProvider />',
});
