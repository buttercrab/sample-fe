import { createContext } from '../../utils/create-context';
export const [ToggleGroupProvider, useToggleGroupContext] = createContext({
    hookName: 'useToggleGroupContext',
    providerName: '<ToggleGroupProvider />',
});
