import { createContext } from '../../utils/create-context';
export const [PinInputProvider, usePinInputContext] = createContext({
    hookName: 'usePinInputContext',
    providerName: '<PinInputProvider />',
});
