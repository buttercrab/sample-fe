import { createContext } from '../../utils/create-context';
export const [StepsItemPropsProvider, useStepsItemPropsContext] = createContext({
    hookName: 'useStepsItemPropsContext',
    providerName: '<StepsItemPropsProvider />',
});
