import { createContext } from '../../utils/create-context';
export const [SliderProvider, useSliderContext] = createContext({
    hookName: 'useSliderContext',
    providerName: '<SliderProvider />',
});
