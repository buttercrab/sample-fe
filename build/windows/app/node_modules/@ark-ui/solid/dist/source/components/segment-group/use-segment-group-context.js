import { createContext } from '../../utils/create-context';
export const [SegmentGroupProvider, useSegmentGroupContext] = createContext({
    hookName: 'useSegmentGroupContext',
    providerName: '<SegmentGroupProvider />',
});
