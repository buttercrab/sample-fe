import { JSX } from "solid-js";
declare function CustomPortal<T extends boolean = false, S extends boolean = false>(props: {
    mount?: Node;
    useShadow?: T;
    isSVG?: S;
    ref?: (S extends true ? SVGGElement : HTMLDivElement) | ((el: (T extends true ? {
        readonly shadowRoot: ShadowRoot;
    } : {}) & (S extends true ? SVGGElement : HTMLDivElement)) => void);
    overlayChildren?: JSX.Element;
    isModal?: boolean;
    children: JSX.Element;
}): Text;
export default CustomPortal;
