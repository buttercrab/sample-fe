import type { JSX } from 'solid-js';
import { type UseMenuItemContext } from './use-menu-item-context';
export interface MenuItemContextProps {
    children: (context: UseMenuItemContext) => JSX.Element;
}
export declare const MenuItemContext: (props: MenuItemContextProps) => JSX.Element;
