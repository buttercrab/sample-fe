import { createContext } from '../../utils/create-context';
export const [QrCodeProvider, useQrCodeContext] = createContext({
    hookName: 'useQrCodeContext',
    providerName: '<QrCodeProvider />',
});
