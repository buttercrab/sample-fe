import { createContext } from '../../utils/create-context';
export const [RatingGroupProvider, useRatingGroupContext] = createContext({
    hookName: 'useRatingGroupContext',
    providerName: '<RatingGroupProvider />',
});
