import { createContext } from '../../utils/create-context';
export const [SelectItemPropsProvider, useSelectItemPropsContext] = createContext({
    hookName: 'useSelectItemPropsContext',
    providerName: '<SelectItemPropsProvider />',
});
