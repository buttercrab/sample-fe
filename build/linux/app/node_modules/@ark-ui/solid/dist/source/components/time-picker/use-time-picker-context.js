import { createContext } from '../../utils/create-context';
export const [TimePickerProvider, useTimePickerContext] = createContext({
    hookName: 'useTimePickerContext',
    providerName: '<TimePickerProvider />',
});
