import { createContext } from '../../utils/create-context';
export const [HoverCardProvider, useHoverCardContext] = createContext({
    hookName: 'useHoverCardContext',
    providerName: '<HoverCardProvider />',
});
