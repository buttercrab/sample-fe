import { createContext } from '../../utils/create-context';
export const [MenuItemGroupProvider, useMenuItemGroupContext] = createContext({
    hookName: 'useMenuItemGroupContext',
    providerName: '<MenuItemGroupProvider />',
});
