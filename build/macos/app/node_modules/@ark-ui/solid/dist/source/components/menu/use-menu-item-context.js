import { createContext } from '../../utils/create-context';
export const [MenuItemProvider, useMenuItemContext] = createContext({
    hookName: 'useMenuItemContext',
    providerName: '<MenuItemProvider />',
});
