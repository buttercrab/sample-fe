import { createContext } from '../../utils/create-context';
export const [DatePickerTableProvider, useDatePickerTableContext] = createContext({
    hookName: 'useDatePickerTableContext',
    providerName: '<DatePickerTableProvider />',
});
