import { createContext } from '../../utils/create-context';
export const [SwitchProvider, useSwitchContext] = createContext({
    hookName: 'useSwitchContext',
    providerName: '<SwitchProvider />',
});
