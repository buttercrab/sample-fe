import { createContext } from '../../utils/create-context';
export const [TreeViewDepthProvider, useTreeViewDepthContext] = createContext({
    hookName: 'useTreeViewDepthContext',
    providerName: '<TreeViewDepthProvider />',
});
