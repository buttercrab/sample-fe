import { createContext } from '../../utils/create-context';
export const [CarouselProvider, useCarouselContext] = createContext({
    hookName: 'useCarouselContext',
    providerName: '<CarouselProvider />',
});
