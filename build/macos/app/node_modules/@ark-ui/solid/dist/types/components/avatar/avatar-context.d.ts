import type { JSX } from 'solid-js';
import { type UseAvatarContext } from './use-avatar-context';
export interface AvatarContextProps {
    children: (context: UseAvatarContext) => JSX.Element;
}
export declare const AvatarContext: (props: AvatarContextProps) => JSX.Element;
